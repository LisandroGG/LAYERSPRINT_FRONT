const Loading = () => {
	return (
		<div className="flex h-full flex-col items-center justify-center">
			<p className="font-mono text-md text-muted">Cargando...</p>
		</div>
	);
};

export default Loading;
